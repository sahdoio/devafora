# Always install devDependencies (Vite, Tailwind, plugins) even when the build
# runs with NODE_ENV=production (e.g. Laravel Cloud). Without this, `npm ci`
# omits dev deps and `npm run build` fails with "vite: not found".
# Node deps are only used to build assets, never at PHP runtime, so this is safe.
include=dev
